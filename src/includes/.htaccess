# includes/ フォルダはPHPのinclude/require経由でのみ使用するため、
# ブラウザからの直接アクセスをすべて拒否する
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>